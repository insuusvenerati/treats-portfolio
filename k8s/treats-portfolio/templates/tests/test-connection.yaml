apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "treats-portfolio.fullname" . }}-test-connection"
  labels:
    {{- include "treats-portfolio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "treats-portfolio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
